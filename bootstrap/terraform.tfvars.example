# Copy to terraform.tfvars and edit. Do NOT commit the real file.
# The bucket name must be globally unique across all of AWS.

aws_region        = "eu-west-1"
state_bucket_name = "acme-tfstate-123456789012"
